public class flywithwings implements flybehavior {
    public flywithwings() {
    }

    public boolean getFly() {
        return Boolean.parseBoolean("the bird has wings so the bird can fly");
    }
}
